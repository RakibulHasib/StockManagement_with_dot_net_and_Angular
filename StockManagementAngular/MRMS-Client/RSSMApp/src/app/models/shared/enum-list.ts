export enum medicalStatus { Fit = 1, Unfit, Pending }

export enum issueStatus {Pending = 1,Resolved, Cancelled }

export enum gender { Male = 1, Female, Others }

export enum religion { Islam = 1, Hinduism, Buddhism, Christianity, Others }

export enum maritalStatus { Married = 1, UnMarried, Others }

export enum eduction {SSC = 1, HSC, UnderGraduation, Masters, Others }
