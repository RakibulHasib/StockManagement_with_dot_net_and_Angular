import { DailyDistributionModel } from './daily-distribution-model';

describe('DailyDistributionModel', () => {
  it('should create an instance', () => {
    expect(new DailyDistributionModel()).toBeTruthy();
  });
});
